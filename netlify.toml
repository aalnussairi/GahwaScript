[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter gahwascript build && pnpm --filter @gahwascript/web build"
  publish = "packages/web/dist"

[build.environment]
  NODE_VERSION = "22.12.0"
